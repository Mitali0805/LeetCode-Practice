const validPalindrome = (num) => {
    if(num < 0) return false
    
    // console.log(num.reverse)
    for(let i= num.length-1; i>=0; i--){
    }
}

const output = validPalindrome(121);
console.log(output)